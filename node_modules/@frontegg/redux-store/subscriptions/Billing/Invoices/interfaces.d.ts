import { FronteggApiError } from '@frontegg/rest-api';
import { Invoice } from '../../interfaces';
export interface InvoicesState {
    loading: boolean;
    error: FronteggApiError | null;
    fetching: boolean;
    invoices: Invoice[];
    invoiceDownload: {
        loading: boolean;
        error: FronteggApiError | null;
    };
}
export interface InvoicesActions {
    loadInvoices: () => void;
    downloadInvoice: (payload: DownloadInvoiceActionPayload) => void;
}
export interface DownloadInvoiceActionPayload {
    invoiceId: string;
    filename: string;
}
