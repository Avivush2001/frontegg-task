import { FronteggApiError } from '@frontegg/rest-api';
export interface StripeState {
    loading: boolean;
    error: FronteggApiError | null;
    customerId?: string;
    cardSetupIntentSecret: string | null;
}
export interface StripeActions {
    createCardSetupIntentSecret: (paymentMethodId: PaymentMethodId) => void;
}
export type PaymentMethodId = string | null;
