import { ISubTenant, ISubTenantTree, ISwitchTenant, ITenantsResponseV2 } from '@frontegg/rest-api';
import { WithCallback } from '../../interfaces';
export type { ISubTenant };
export interface TenantsState {
    tenants: ITenantsResponseV2[];
    loading: boolean;
    switchingTenant: boolean;
    error?: any;
    subTenants: ISubTenant[];
    tenantTree: ISubTenantTree | null;
    activeTenant?: ITenantsResponseV2;
}
export type SwitchTenantOptions = WithCallback<ISwitchTenant>;
