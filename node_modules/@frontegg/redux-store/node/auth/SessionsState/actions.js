"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _helpers = require("../../helpers");
var _state = require("./state");
var _default = (store, api, sharedActions) => {
  const setSessionsState = state => {
    Object.assign(store.auth.sessionsState, state);
  };
  const resetSessionsState = () => {
    (0, _helpers.deepResetState)(store, ['auth', 'sessionsState'], _state.initialState);
  };
  const loadUserSessions = async payload => {
    setSessionsState({
      loading: true,
      error: null
    });
    try {
      const sessions = await (0, _helpers.retryIfNeeded)(() => api.auth.getCurrentUserSessions(), payload == null ? void 0 : payload.retryConfig);
      setSessionsState({
        sessions,
        loading: false
      });
    } catch (e) {
      setSessionsState({
        error: (0, _helpers.errorHandler)(e),
        loading: false
      });
    }
  };
  const loadCurrentUserSession = async () => {
    setSessionsState({
      loading: true,
      error: null
    });
    try {
      const session = await api.auth.getCurrentUserSession();
      setSessionsState({
        currentSession: session,
        loading: false
      });
    } catch (e) {
      setSessionsState({
        error: (0, _helpers.errorHandler)(e),
        loading: false
      });
    }
  };
  const deleteUserSession = async payload => {
    const {
      id
    } = payload;
    setSessionsState({
      loading: true,
      error: null
    });
    try {
      await api.auth.deleteSessionForUser(id);
      await loadUserSessions();
    } catch (e) {
      setSessionsState({
        error: (0, _helpers.errorHandler)(e),
        loading: false
      });
    }
  };
  const deleteAllUserSessions = async () => {
    setSessionsState({
      loading: true,
      error: null
    });
    try {
      await api.auth.deleteAllSessionsForUser();
      await loadUserSessions();
    } catch (e) {
      setSessionsState({
        error: (0, _helpers.errorHandler)(e),
        loading: false
      });
    }
  };
  const revokeUserSessions = async payload => {
    const {
      callback,
      userId
    } = payload;
    setSessionsState({
      loading: true,
      error: null
    });
    try {
      await api.auth.revokeSessionsForUser(userId);
      setSessionsState({
        loading: false
      });
      callback == null ? void 0 : callback(true);
    } catch (e) {
      setSessionsState({
        error: (0, _helpers.errorHandler)(e),
        loading: false
      });
      callback == null ? void 0 : callback(false);
    }
  };
  return {
    setSessionsState,
    resetSessionsState,
    loadUserSessions,
    loadCurrentUserSession,
    deleteUserSession,
    deleteAllUserSessions,
    revokeUserSessions
  };
};
exports.default = _default;