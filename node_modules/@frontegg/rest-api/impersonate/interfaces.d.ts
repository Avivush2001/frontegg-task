export type IImpersonate = {
    impersonatedUserId: string;
    actorToken: string;
    redirectUrl?: string;
};
export type IImpersonateResponse = {
    accessToken: string;
    tokenType: string;
    refreshToken: string;
    expiresIn: number;
    expires: string;
};
