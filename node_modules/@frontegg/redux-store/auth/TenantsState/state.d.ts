import { TenantsState } from './interfaces';
export declare const initialState: TenantsState;
declare const _default: (overrideState?: Partial<TenantsState>) => TenantsState;
export default _default;
