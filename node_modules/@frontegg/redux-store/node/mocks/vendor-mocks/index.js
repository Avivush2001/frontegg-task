"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.buildVendorActions = void 0;
var _vendor = require("../../vendor");
var _helpers = require("../helpers");
var _helpers2 = require("../../helpers");
const buildVendorActions = (store, api, actions) => {
  const originalActions = (0, _vendor.buildVendorActions)(store, api, actions);
  const mockedActions = (0, _helpers.mockActionsExpect)(originalActions, ['setVendorState', 'resetVendorState']);
  mockedActions.loadVendorPublicInfo = async () => {
    try {
      mockedActions.setVendorState({
        whiteLabelMode: false
      });
      await (0, _helpers2.delay)();
    } catch (e) {
      console.error('failed to getVendorPublicInfo - ', e);
    }
  };
  mockedActions.loadVendorPublicConfiguration = mockedActions.loadVendorPublicInfo;
  return mockedActions;
};
exports.buildVendorActions = buildVendorActions;