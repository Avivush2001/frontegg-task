import { FronteggState, RestApi, SharedActions } from '../../interfaces';
import { SubscriptionsActions, SubscriptionsStateActions } from '../../subscriptions';
export declare const buildSubscriptionsActions: (store: FronteggState, api: RestApi, actions: SharedActions) => [SubscriptionsActions, SubscriptionsStateActions];
